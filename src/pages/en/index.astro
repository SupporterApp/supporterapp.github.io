---
import Layout from '../../layouts/Layout.astro';
import TeamScores from '../../components/TeamScores.astro';
---

<Layout title="Ffans - Football Fans">
  <div class="hero">
    <h1 class="main-title">Ffans: Football Fans</h1>
    <p class="subtitle">You are the soul of football. Now you have your own score.</p>
    <p class="highlight">The passion of the fans had never been measured. Until now.</p>
    <p class="description">
      Ffans is the first app that puts you, the fan, at the center of the show.
      There are no results or signings. There are only people like you, who live football with intensity.
    </p>
  </div>

  <TeamScores lang="en" />
</Layout>

<style>
  .hero {
    display: flex;
    flex-direction: column;
    justify-content: center;
    min-height: 40vh;
    text-align: center;
    padding-bottom: 1rem;
  }
  .main-title {
    font-size: 3.5rem;
    font-weight: 800;
    margin-bottom: 0.5rem;
    color: var(--text);
    text-shadow: 0 2px 8px var(--hero-title-shadow);
  }
  .subtitle {
    font-size: 1.8rem;
    font-weight: 300;
    margin-bottom: 0.5rem;
    color: var(--text);
  }
  .highlight {
    font-size: 1.4rem;
    font-weight: 600;
    color: var(--highlight);
    margin-bottom: 1.5rem;
  }
  .description {
    font-size: 1.2rem;
    line-height: 1.8;
    max-width: 700px;
    margin: 0 auto;
    color: var(--text);
  }
  @media (max-width: 720px) {
    .hero {
      min-height: 30vh;
    }
    .main-title {
      font-size: 2.2rem;
    }
    .subtitle {
      font-size: 1.2rem;
    }
  }
</style>
